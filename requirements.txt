# Package versions needed to run this code. Installed with:
# pip install -r requirements.txt
# These are reasonable working versions. For an exact match to the
# environment used to generate the results in the dissertation, I
# recommend running `pip freeze > requirements.txt` in that same
# environment and keeping the lines for the packages listed below.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
imbalanced-learn>=0.11
shap>=0.44
joblib>=1.3
matplotlib>=3.7
scipy>=1.11
